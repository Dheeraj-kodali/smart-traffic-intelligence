# Voice Assistant Setup

To use the voice assistant module on Windows, you need to install the following dependencies:

```bash
pip install SpeechRecognition pyttsx3
pip install pipwin
pipwin install pyaudio
```
